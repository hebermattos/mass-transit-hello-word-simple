version: '3'

services:

  receiver:
    build: mt-receive/ .
    depends_on:
      - sender
      - fila

  sender:
    build: mt-send/ .
    depends_on:
      - fila

  fila:
    image: "rabbitmq:3-management"
    ports:
    - "15672:15672"
    - "5672:5672"